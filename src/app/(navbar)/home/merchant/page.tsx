import Logout from "@/app/_components/logout";
import React from "react";

const page = () => {
  return (
    <div>
      <div>
        <Logout />
      </div>
    </div>
  );
};

export default page;
